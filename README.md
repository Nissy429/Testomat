# BDD Automation Framework (Cucumber + Java + Selenium)

This is a complete Behavior-Driven Development (BDD) automation framework using **Java**, **Selenium WebDriver**, **Cucumber**, **JUnit**, and **Maven**.  
It includes feature files, step definitions, and a test runner to execute automated UI tests.

---

## 📁 Project Structure

